<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Your Order</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(to right, #ff9966, #ff5e62);
            margin: 0;
            padding: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            color: white;
            text-align: center;
        }

        .container {
            background: #fff3e0;
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
            width: 90%;
            max-width: 450px;
            text-align: center;
        }

        h1 {
            font-size: 24px;
            font-weight: 600;
            color: #ff5722;
            text-align: center;
            margin-bottom: 10px;
        }

        .emoji {
            font-size: 30px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 100%;
        }

        label {
            font-size: 16px;
            font-weight: 600;
            color: #444;
            display: block;
            margin: 10px 0 5px;
            text-align: left;
            width: 90%;
        }

        input, textarea {
            width: 90%;
            padding: 10px;
            border: 2px solid #ccc;
            border-radius: 8px;
            font-size: 14px;
            background: white;
            transition: 0.3s ease;
        }

        input:focus, textarea:focus {
            border-color: #ff5722;
            outline: none;
            box-shadow: 0 0 8px rgba(255, 87, 34, 0.3);
        }

        textarea {
            height: 100px;
            resize: none;
        }

        .btn {
            width: 90%;
            padding: 12px;
            margin-top: 15px;
            background: #ff5722;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            transition: 0.3s ease;
        }

        .btn:hover {
            background: #e64a19;
            transform: scale(1.05);
        }

        .icon {
            font-size: 18px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="emoji">✏️ Edit Your Order</h1>
        
        <form action="/posts/<%= post.id %>?_method=PATCH" method="POST">
            <div class="form-group">
                <label for="username">Your Name:</label>
                <input type="text" id="username" name="username" value="<%= post.username %>" required>
            </div>

            <div class="form-group">
                <label for="content">Your Order:</label>
                <textarea id="content" name="content" required><%= post.content %></textarea>
            </div>

            <button type="submit" class="btn">
                <span class="icon">✅</span> Update Order
            </button>
        </form>
    </div>
</body>
</html>

